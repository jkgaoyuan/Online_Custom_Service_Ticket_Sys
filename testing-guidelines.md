# 测试用例生成规范

> 新增模块或补充测试时，遵循本规范可保证用例设计的一致性、完整性和可执行性。  
> 技术栈无关：后端可以是任意语言/框架，前端可以是任意组件库。

---

## 一、测试设计方法（必须遵循）

| 方法 | 定义 | 适用场景 | 示例 |
|------|------|----------|------|
| **等价类划分法** | 将输入域划分为有效/无效等价类，每类至少选 1 个代表值 | 所有输入参数 | 用户名存在（有效）vs 不存在（无效） |
| **边界值分析法** | 对数值/字符串/时间字段测试 min、min+1、max、max+1、空值、null | 有长度/范围限制的字段 | 字符串 255/256 字符、负数 ID、空数组 |
| **场景法** | 基于业务流程设计基本流、备选流、异常流 | 端到端、权限、状态机 | 正常下单 → 库存不足 → 支付超时 |
| **错误推测法** | 基于经验推测竞态、重复提交、网络中断等易错点 | 并发、交互、异常恢复 | 并发扣款、快速连续点击、乐观更新回滚 |

---

## 二、优先级定义

| 优先级 | 范围 | 判定标准 | 示例 |
|--------|------|----------|------|
| **P0** | 核心业务流正向 + 阻断性异常 | 功能不可用、数据不一致、安全漏洞 | 未认证 401、不存在 404、非法状态 409 |
| **P1** | 边界值 + 重要异常 + 数据联动 + 安全基础 | 影响用户体验或数据正确性 | 空对象/空数组、XSS、Token 篡改 |
| **P2** | 性能基线 + 兼容性 + 极端边界 + 非阻断性优化 | 锦上添花，不阻塞发布 | 慢网加载、大数据量渲染、内存泄漏 |

**执行原则**：新增模块时，P0 必须全部覆盖，P1 覆盖主要场景，P2 视时间补充。

---

## 三、用例 ID 命名体系

| 类型 | 格式 | 示例 | 说明 |
|------|------|------|------|
| 功能用例 | `TC-{MODULE}-{SEQ:03d}` | `TC-USER-001`, `TC-ORDER-042` | 端到端功能场景 |
| 接口用例 | `API-{MODULE}-{SEQ:03d}` | `API-USER-005`, `API-ORDER-013` | 单接口请求/响应校验 |
| 并发用例 | `TC-{MODULE}-CON-{SEQ:03d}` | `TC-ORDER-CON-001` | 竞态条件专项 |
| 前端用例 | `TC-FE-{SEQ:03d}` | `TC-FE-013`, `TC-FE-058` | 前端组件/页面交互 |
| 前端专项 | `TC-FE-SPEC-{SEQ:03d}` | `TC-FE-SPEC-001` | 跨页面专项（乐观更新、E2E） |

**SEQ 分配规则**：同一模块内连续递增，不跳号。

---

## 四、后端测试规范

### 4.1 文件组织

```
tests/
├── conftest.py              # 全局 fixtures（如需要）
├── test_{module_a}.py       # 按业务模块拆分
├── test_{module_b}.py
├── test_unauthorized.py     # 【专项】批量未认证参数化测试
├── test_concurrency.py      # 【专项】并发竞态
├── test_performance.py      # 【专项】性能基线
└── test_e2e.py              # 【专项】端到端跨模块流程
```

**原则**：单模块测试放在 `test_{module}.py`；跨模块或通用场景放入专项文件。

### 4.2 函数命名规范

```python
# 基础正向
test_{操作}_{对象}_{预期结果}_{状态码}
# 异常/边界
test_{字段}_{无效条件}_{状态码}
# 并发
test_concurrent_{场景}_{预期行为}
# E2E
test_{流程描述}_{验证目标}
```

**示例**：
- `test_login_wrong_password_401`
- `test_title_exceeds_max_length_422`
- `test_concurrent_deduction_race_condition`
- `test_create_pay_refund_status_consistency`

### 4.3 代码模式

#### 模式 1：辅助工厂函数
每个测试文件顶部定义私有工厂函数，避免重复构造数据：

```python
async def _user(db, username, role="member"):
    # 构造并返回用户实体
    ...

def _auth(uid):
    # 返回认证头
    ...

def _entity_body(required_field, optional_overrides=None):
    # 返回标准请求体，支持字段覆盖
    ...
```

#### 模式 2：数据准备
在测试函数内直接构造数据，不依赖外部种子文件，保证测试独立：

```python
async def test_create_entity_success(client, db):
    user = await _user(db, "creator")
    r = await client.post("/api/entities", headers=_auth(user.id),
                          json=_entity_body(required_field="value"))
    assert r.status_code == 201
```

#### 模式 3：断言风格
至少同时断言 **状态码 + 具体字段/错误码**，不只做状态码检查：

```python
assert r.status_code == 401
assert r.json()["detail"]["code"] == "INVALID_CREDENTIALS"
```

#### 模式 4：分层注释
不同批次补充的用例用注释分隔，方便追溯：

```python
# ===== 边界值补充（批次1） =====
async def test_field_exceeds_max_length_422(client, db): ...

# ===== 高优先级补充 =====
async def test_field_empty_422(client, db): ...

# ===== 中优先级补充 =====
async def test_optional_field_missing_200(client, db): ...
```

### 4.4 必须覆盖的维度（后端最低要求）

| 维度 | 最低要求 | 检查清单 |
|------|----------|----------|
| 正向流程 | 基本流 + 1 条备选流 | 创建成功、列表查询、详情查看 |
| 边界值 | 字符串长度、数值正负零、空数组/空对象、过去时间 | 最大值+1、负数、空数组、空对象 |
| 异常 | 404（不存在）、409（状态冲突）、422（校验失败） | 资源不存在、非法状态流转、字段校验失败 |
| 权限 | 未认证 401、越权 403、角色不匹配 | 未登录访问、低角色访问高权限接口 |
| 安全 | SQL 注入、XSS、Token 篡改/过期/伪造格式 | 注入语句、脚本标签、错误签名 JWT |
| 并发 | 同名创建竞态、同一资源并发修改、终态保护 | 多协程同时请求、终态后禁止修改 |
| E2E | 至少 1 条跨模块完整业务流程 | 创建→流转→操作→统计一致性验证 |

---

## 五、前端测试规范

### 5.1 文件组织

```
frontend/tests/
├── api/
│   ├── client.test.*          # API 客户端（错误处理、JSON 解析）
│   └── hooks.test.*           # 数据层 hooks（缓存、乐观更新）
├── stores/
│   └── auth.test.*            # 全局状态管理（如果适用）
├── lib/
│   └── utils.test.*           # 纯工具函数（排序、过滤、格式化）
├── components/
│   └── {Component}.test.*     # 通用组件
└── pages/
    └── {Page}.test.*          # 页面级组件
```

### 5.2 命名规范

```typescript
describe('{模块名} ({用例ID})', () => {
  it('{场景描述}', () => { ... })
})
```

**示例**：
```typescript
describe('Form submit guard (TC-FE-013)', () => {
  it('does not trigger submit when already pending', () => { ... })
})
```

### 5.3 Mock 策略（按依赖类型）

| 依赖 | Mock 方式 |
|------|-----------|
| 数据层 Hooks | `vi.mock('@/api/hooks')` 或对应路径 |
| API 客户端 | `vi.mock('@/api/client')` 或拦截 `globalThis.fetch` |
| 全局状态 | `vi.mock('@/stores/auth')` 或对应状态管理库 |
| 外部交互库 | 部分 mock，保留结构仅替换回调 |
| 浏览器 API | 直接赋值 `globalThis.localStorage = { ... }` |

### 5.4 必须覆盖的维度（前端最低要求）

| 维度 | 最低要求 | 检查清单 |
|------|----------|----------|
| 渲染 | 正常加载、空数据、错误状态 | 表格渲染、错误 Alert、空状态占位 |
| 交互 | 点击、提交、筛选、跳转 | 按钮点击、表单提交、下拉筛选 |
| 权限 | 路由拦截、菜单隐藏、按钮权限 | 未授权路由跳转、敏感按钮隐藏 |
| Loading Guard | 提交中禁用、重复点击忽略、pending 拦截 | 按钮 loading、快速双击只发一次请求 |
| 乐观更新 | 先更新 UI 再请求，失败回滚 | 缓存先变、API 失败后恢复原值 |
| Fallback | 未知枚举值显示原始字符串、无效 ID 处理 | 新枚举值不显示 undefined、非法路由提示 |
| 数据联动 | 操作后全局状态变化、列表刷新 | 操作后通知计数变化、新增后列表出现 |

---

## 六、特殊场景处理（通用）

| 场景 | 后端要求 | 前端要求 |
|------|----------|----------|
| **乐观更新** | 返回明确状态码（200/409/422），不隐式成功 | 测试缓存更新 + 失败回滚两条路径 |
| **Token 过期** | 签发过期 Token 测试 401；禁用用户后旧 token 失效 | 测试 401 触发登出 + 跳转登录页 |
| **并发测试** | 多协程/多线程同时请求同一资源 | 模拟快速连续点击，验证 pending guard |
| **空对象/空数组** | 区分 `{}`、`[]`、`null`、字段缺失四种情况 | 表单提交空值校验 |
| **数值边界** | 测试负数、零、极大值 | 表单输入非法数值的校验提示 |
| **通知/消息联动** | 操作后验证通知/消息生成；自操作不发通知 | 下拉列表、Badge 计数、点击跳转 |
| **状态机重置** | 回退状态时关联数据是否正确重置 | 时间线/历史记录展示重置事件 |

---

## 七、输出格式要求

### 第一步：用例设计文档

**存放位置**：`docs/testcases/{序号}-{module}-testcases.md`

**文件结构**：
- `00-summary.md`：总览索引、模块覆盖矩阵、遗留风险清单
- `01-{module-a}-testcases.md`：模块 A 功能用例 + 接口用例
- `02-{module-b}-testcases.md`：模块 B
- `03-{module-c}-testcases.md`：模块 C
- `04-frontend-testcases.md`：前端交互用例（跨页面聚合）

**表格字段**：

| 字段 | 说明 |
|------|------|
| 用例 ID | 按第三节命名体系（如 `TC-USER-001`） |
| 测试模块 | 如「用户创建」、「订单支付」 |
| 测试场景 | 一句话描述 |
| 前置条件 | 数据状态、用户角色 |
| 操作步骤 | 1. ... 2. ... |
| 测试数据 | 具体输入值 |
| 预期结果 | 状态码 + 关键字段值 |
| 优先级 | P0 / P1 / P2 |
| 覆盖类型 | 正向 / 异常 / 边界 / 权限 / 安全 / 性能 |
| 设计方法 | 等价类划分 / 边界值分析 / 场景法 / 错误推测法 |

### 第二步：可执行测试代码

将用例设计翻译为代码，要求：

1. **一一映射**：每条设计用例至少对应 1 个测试函数（复杂场景可拆多条）
2. **ID 引用**：代码注释中引用用例 ID
   ```python
   # TC-USER-011: 创建用户时邮箱已存在
   async def test_create_user_duplicate_email_409(client, db): ...
   ```
3. **排序规则**：
   - 全局按 P0 → P1 → P2
   - 同优先级内按：正向 → 异常 → 边界 → 权限 → 安全 → 性能
4. **异步规范**：后端异步接口使用 `async/await`；前端异步 hooks 使用 `async/await` + `waitFor`
5. **专项分离**：并发、性能、E2E、批量未认证不混在模块文件中，放入对应专项文件

---

## 八、快速参考：最小测试集模板

为新增模块时，按以下清单生成最小可用测试集：

### 后端最小集（~15 条）

```
P0 正向（3条）
  - 创建/查询/更新基本流
P0 异常（4条）
  - 404 不存在、409 状态冲突、422 校验失败、401 未认证
P1 边界（4条）
  - 字符串 max+1、负数、空数组、空对象
P1 权限（2条）
  - 越权访问、角色不匹配
P1 安全（2条）
  - SQL 注入、XSS 或 Token 篡改
```

### 前端最小集（~10 条）

```
P0 渲染（2条）
  - 正常加载、空数据/错误状态
P0 交互（2条）
  - 核心操作（提交/点击）、跳转
P0 权限（1条）
  - 路由拦截或按钮隐藏
P1 Loading Guard（2条）
  - 提交中禁用、重复点击忽略
P1 Fallback（1条）
  - 未知枚举值显示原始值
P1 数据联动（2条）
  - 操作后通知/列表刷新
```

---

## 九、修订记录

| 日期 | 修订人 | 内容 |
|------|--------|------|
| 2026-08-03 | — | 通用初版：去业务化、去技术栈绑定 |
